import { MiniApp } from "@farcaster/miniapps-sdk";

const miniApp = new MiniApp({
  appId: "Memory-minter",
  displayName: "Memory App",
  description: "Write and mint your memories on Base",
});

export default miniApp;
